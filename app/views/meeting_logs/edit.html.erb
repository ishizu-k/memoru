<h3><%= t('action.edit') %></h3>

<div class="lists">
  <div class="box-create-list">
    <%= form_with(model: @meeting_log, local: true) do |f| %>
      <%= render partial: "form", locals: {f: f} %>
      <%= f.submit t('action.edit'), class: "btn outline btn-primary" %>
    <% end %>
  </div>
</div>
