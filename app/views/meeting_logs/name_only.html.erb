<%= render partial: "menu" %>

<div class="lists">
  <ul>
    <% @meeting_logs.each do |m| %>
      <div class="list-learn">
        <table class="table-list">
          <tr><td><%= link_to (m.name), meeting_log_path(m.id) %> さん</td></tr>
          <tr><td>
            <details>
              <summary>画像を表示する</summary>
              <%= image_tag m.image.url %><br>
              <%= m.position %>
            </details>
          </td></tr>
          <tr><td></td></tr>
        </table>
      </div>
    <% end %>
  </ul>

  <%= paginate @meeting_logs %>
</div>
