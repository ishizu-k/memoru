<%= form_with(model: @tag, local: true) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, t('meeting_log.tag') %>
  <%= f.text_field :name %>

  <%= f.submit t('action.create') %>
<% end %>
