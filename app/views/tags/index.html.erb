<h3>タグの管理画面</h3>

<table>
  <% @tags.each do |t| %>
    <tr>
      <td><%= t.name %></td>
      <td><%= link_to "削除", tag_path(t.id), method: :delete, data: {confirm: "タグを削除しますか？"} %></td>
    </tr>
  <% end %>
</table>

<br>

<%= render "new" %>
